# Pod Identity service account. The role is bound AWS-side via an EKS Pod
# Identity association (namespace=tansu-m2, sa=tansu-m2) → an IAM role with
# access to s3://eks-mb-sandbox-tansu-917630917049. No IRSA annotation needed.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tansu-m2
